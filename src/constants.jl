
"""Electron-Volts in one atomic unit."""
const eV_per_au = 27.211; # eV

"""Electron-Volt per Joule."""
const joule = 1.0/1.602e-19; # eV

"""hbar in atomic units."""
const hbar = 1.0;

"""Planck's constant in atomic units."""
const h = 2π; # planck constant (hbar = 1)


"""Fine structure constant, alpha_QED."""
const alpha = 1.0/137.0;

"""Speed of light in atomic units."""
const c = 137.0;

"""Femto-seconds in one atomic unit."""
const fs_per_au = 2.418e-2;

"""Nano-meter in one atomic unit."""
const nm_per_au = 5.291e-2;

export eV_per_au;
export c;
export fs_per_au;
export nm_per_au;
